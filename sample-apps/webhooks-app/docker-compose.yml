version: '3.5'

services:
  web:
    container_name: python-webhooks-app-web
    env_file:
      - .env
    build:
      dockerfile: ./sample-apps/webhooks-app/docker/web/Dockerfile
      context: ../../
    ports:
      - 5000:5000
    volumes:
      - ./src:/app/src
